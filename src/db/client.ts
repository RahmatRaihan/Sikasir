// Database client — SiKasir DWP RS Rubini
// Re-exports the Supabase client as the main database interface
export { supabase } from './supabaseClient';
export { supabase as default } from './supabaseClient';
